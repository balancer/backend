query allPools($chainIds: [Int!]) {
    allPools(chainIds: $chainIds) {
        ...PoolSchema
    }
}

fragment PoolSchema on PoolSchema {
    id
    chainId
    address
    isShutdown
    aprs {
        total
    }
    lpToken {
        address
    }
}
