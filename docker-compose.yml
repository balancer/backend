version: '3'

services:
    postgres:
        image: postgres
        ports:
            - '5431:5432'
        environment:
            POSTGRES_USER: beetx
            POSTGRES_PASSWORD: let-me-in
            POSTGRES_DB: beetx
